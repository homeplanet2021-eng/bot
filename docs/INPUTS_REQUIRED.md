# Требуемые входные данные

## Remnawave
- UUID нод для всех серверов в `config/servers.yml`, кроме NL (см. placeholders `<TODO_NODE_UUID>`).
- Подтвержденный контракт Remnawave 2.5.7 (эндпоинты, поля, ошибки) из официальной документации.

## Telegram Stars
- Проверка актуальных ограничений Stars (invoice payload, refund API) по первоисточникам.

## Операции/Инфра
- Реальные публичные хосты серверов.
- Параметры емкости `max_active_slots` по фактическим лимитам.
- Секреты для prod/staging (вне репозитория).
