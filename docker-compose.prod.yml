version: "3.9"

services:
  bot:
    build: .
    env_file:
      - .env.example
    depends_on:
      - postgres
      - redis
    networks:
      - default
      - remnawave-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vpn-bot.rule=Host(`bot.horizonid.space`) && PathPrefix(`/webhooks/telegram`)"
      - "traefik.http.routers.vpn-bot.entrypoints=websecure"
      - "traefik.http.routers.vpn-bot.tls=true"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: vpn_bot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - default

  redis:
    image: redis:7
    networks:
      - default

networks:
  remnawave-network:
    external: true
